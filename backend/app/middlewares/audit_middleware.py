"""操作审计日志中间件：对写操作自动记录操作日志。

当前版本由各蓝图内部显式调用 _log()（auth.py 已实现）。
本文件留作后续统一审计入口的扩展点。

如需全局自动审核，可在 after_request 钩子中根据 response 状态码
和 request.method 自动生成日志，但有粒度控制挑战（难以知晓"是改了什么设备"）。
目前推荐的方案是：关键操作在业务代码内显式记录（精细）。
"""
# 占位，后续可扩展全局审计自动记录

# 实验室设备管理系统 —— 功能测试清单

> 测试环境：`https://49.233.177.123`  
> 测试账号：`superadmin` / `你的超管密码`  
> 每个用例标 ✅ 通过 或 ❌ 失败并截图问题

---

## 一、认证模块

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 1.1 | 正常登录 | 输入正确用户名+密码+验证码，点登录 | 登录成功，跳转首页，侧边栏完整显示 | |
| 1.2 | 错误密码 | 输入正确用户名+错误密码 | 提示"用户名或密码错误" | |
| 1.3 | 不存在的用户 | 输入不存在的用户名 | 提示"用户名或密码错误"，不暴露用户不存在 | |
| 1.4 | 空表单提交 | 用户名/密码都不填点登录 | 前端阻止，提示"请输入用户名" | |
| 1.5 | 错误验证码 | 输入正确账号密码，但验证码故意输错 | 提示"验证码错误" | |
| 1.6 | 验证码过期 | 等超过1分钟后用旧验证码登录 | 提示"验证码已过期，请刷新" | |
| 1.7 | 登录限流 | 1分钟内连续发送超过5次登录请求 | 第6次起被限制，返回限流提示 | |
| 1.8 | 账号锁定 | 同一个账号连续输错密码5次 | 账号被锁定，提示"账号已锁定，请约X分钟后再试" | |
| 1.9 | 锁定后正确密码也登不上 | 账号锁定后立即用正确密码登录 | 仍提示锁定，无法登录 | |
| 1.10 | 注册新用户 | 点"去注册"，填完整信息注册 | 注册成功，提示"注册成功" | |
| 1.11 | 重复用户名注册 | 用已存在的用户名再次注册 | 提示"用户名已被使用" | |
| 1.12 | 弱密码注册 | 密码只填"123" | 被后端拒绝，提示密码不符合复杂度要求 | |
| 1.13 | 注册限流 | 1分钟内连续发送超过3次注册请求 | 第4次起被限制 | |
| 1.14 | 修改密码 | 登录后，正确旧密码+新密码修改 | 修改成功，提示重新登录 | |
| 1.15 | 旧密码错误修改 | 旧密码故意填错 | 提示"旧密码错误" | |
| 1.16 | 退出登录 | 点右上角"退出登录"确认 | 退出成功，跳转登录页，无法直接访问首页 | |
| 1.17 | 未登录访问内部页 | 退出后直接在地址栏输入 `/#/device/list` | 自动跳转登录页 | |
| 1.18 | 密码框不自动填充 | 检查登录/注册页密码框 HTML 属性 | `autocomplete="new-password"`，浏览器不自动填充 | |

---

## 二、权限控制

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 2.1 | 超管能访问所有页面 | 登录 superadmin，逐个打开所有侧边栏菜单 | 全部可见，无一报 403 | |
| 2.2 | 普通用户看不到管理菜单 | 注册一个新用户登录，看侧边栏 | 只有"申请借用""我的记录"，没有设备管理/用户管理/操作日志 | |
| 2.3 | 普通用户直接访问管理URL | 用普通用户登录后，直接地址栏输入 `/#/device/list` | 跳转 403 页面 | |
| 2.4 | 普通用户调用删除设备的API | 浏览器 Console 发一个 DELETE 请求删除设备 | 后端返回 403 "无此操作权限" | |
| 2.5 | 修改URL参数查看他人数据 | 借用记录页尝试修改URL参数访问他人记录 | 后端只返回本人的记录（record:self 权限） | |

---

## 三、设备管理

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 3.1 | 查看设备列表 | 打开"设备管理→设备列表" | 显示所有设备，含编号/名称/分类/状态 | |
| 3.2 | 空列表状态 | 如果列表无数据 | 显示 `el-empty` "暂无设备数据"，不是白屏 | |
| 3.3 | 添加设备 | 点"添加设备"，填完整信息提交 | 设备新增成功，列表刷新可见 | |
| 3.4 | 添加设备必填校验 | 不填设备名称直接提交 | 前端提示必填 | |
| 3.5 | 编辑设备 | 点某设备的"编辑"，修改名称后保存 | 修改成功 | |
| 3.6 | 删除设备 | 点某设备的"删除"确认 | 删除成功，列表不再显示 | |
| 3.7 | 按分类筛选 | 用分类下拉框筛选 | 只显示对应分类的设备 | |
| 3.8 | 按状态筛选 | 筛选"已借出"设备 | 只显示状态为"已借出"的设备 | |
| 3.9 | 分页功能 | 设备超过每页条数时 | 底部显示分页器，可翻页 | |
| 3.10 | 管理设备分类 | 打开"设备管理→设备分类" | 显示分类列表，可新增/编辑/删除 | |

---

## 四、借用管理

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 4.1 | 申请借用 | 用普通用户登录，选一台"可借用"的设备申请 | 申请提交成功 | |
| 4.2 | 不能借用已借出设备 | 选一台状态"已借出"的设备点申请 | 提示该设备不可借用 | |
| 4.3 | 借用数量上限 | 连续申请超过3台设备 | 第4次申请时提示已达借用上限 | |
| 4.4 | 查看我的记录 | 打开"借用管理→我的记录" | 只显示本人的借用记录 | |
| 4.5 | 管理员审批通过 | 超管登录，打开"借用管理→审批管理"，点"通过" | 审批通过，设备状态变为"已借出" | |
| 4.6 | 管理员审批拒绝 | 对另一条申请点"拒绝" | 审批拒绝，记录状态变为"已拒绝" | |
| 4.7 | 确认归还 | 管理员对已借出的记录点"确认归还" | 归还成功，设备状态恢复为"可借用" | |
| 4.8 | 逾期标记 | 找一条超过30天未还的记录 | 该行显示红色背景 + 左边框（`row-overdue`） | |
| 4.9 | 事务一致性 | 审批通过时同时改借用状态和设备状态 | 两个状态同时变化，不会出现"审批通过但设备还是可借用" | |

---

## 五、用户管理

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 5.1 | 查看用户列表 | 超管打开"用户管理→用户列表" | 显示所有注册用户 | |
| 5.2 | 空列表状态 | 用户为空时 | 显示空状态提示 | |
| 5.3 | 编辑用户信息 | 点某用户"编辑"，修改真实姓名 | 修改成功 | |
| 5.4 | 禁用用户 | 点"禁用" | 该用户状态变为禁用，无法登录 | |
| 5.5 | 被禁用用户登录 | 用被禁用的账号尝试登录 | 提示"账号已被禁用" | |
| 5.6 | 启用用户 | 对禁用用户点"启用" | 状态恢复，可重新登录 | |
| 5.7 | 超管不能删除自己 | 以 superadmin 身份尝试删除 superadmin | 被阻止，提示"不能删除自己" | |
| 5.8 | 分配角色 | 给普通用户分配"实验室管理员"角色 | 该用户重新登录后侧边栏出现设备管理等菜单 | |

---

## 六、安全运维

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 6.1 | 查看操作日志 | 超管打开"安全运维→操作日志" | 能看到登录/注册/操作等日志记录 | |
| 6.2 | 日志记录真实IP | 查看某条登录日志的 IP 字段 | 是你的真实公网IP，不是 127.0.0.1（证明 ProxyFix 生效） | |
| 6.3 | 日志记录操作详情 | 查看添加设备/审批借用对应的日志 | 记录了操作人、目标对象、详情、时间 | |
| 6.4 | 面包屑导航 | 进入任意二级页面 | 页面顶部显示"首页 > 父分类 > 当前页面"面包屑 | |

---

## 七、CSRF 防护

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 7.1 | 正常写操作带 CSRF | F12 打开 Network，登录后执行任意 POST 操作（如添加设备），检查请求头 | 请求头包含 `X-CSRF-Token: xxx` | |
| 7.2 | 未带 CSRF 被拒 | F12 Console 执行：`fetch('/api/devices',{method:'POST',headers:{'Authorization':'Bearer '+localStorage.getItem('lab_device_token'),'Content-Type':'application/json'},body:JSON.stringify({name:'test'})}).then(r=>r.json()).then(console.log)` | 返回 `code:403`，message 提到"CSRF" | |
| 7.3 | CSRF Token 过期自动刷新 | 等较长时间后执行写操作 | 自动刷新 token 并重试成功，用户无感知 | |

---

## 八、前端健壮性

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 8.1 | 浏览器后退 | 登录后多次操作，点浏览器后退按钮 | 页面不回退到登录页，正常导航 | |
| 8.2 | 刷新页面保持登录 | 在任意内部页面按 F5 刷新 | 保持登录状态，不跳回登录页 | |
| 8.3 | 404 页面 | 地址栏输入不存在的路由如 `/#/abc123` | 显示 404 页面 | |
| 8.4 | Token 过期刷新 | 等 JWT 过期后（默认2小时），刷新页面 | 自动跳转登录页，提示"登录已过期" | |

---

## 九、HTTPS & 部署

| # | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|--------|---------|---------|------|
| 9.1 | HTTP 跳转 HTTPS | 访问 `http://49.233.177.123` | 自动 301 跳转到 `https://49.233.177.123` | |
| 9.2 | HTTPS 可访问 | 访问 `https://49.233.177.123` | 浏览器提示证书警告，点继续后可访问 | |
| 9.3 | 安全头检查 | F12 Network 看任意响应头 | 包含 `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY` | |
| 9.4 | API 反代正常 | 首页正常加载数据 | 看板显示统计数字（非加载失败） | |
| 9.5 | SPA 路由刷新不 404 | 在 `/#/device/list` 页面按 F5 刷新 | 正常显示设备列表，不报 404 | |

---

## 测试用普通账号

> 建议先注册一个测试用普通用户，用于上述 2.2~2.5、4.1~4.4 的测试：
> - 用户名：`tester`
> - 密码：符合复杂度要求的密码

---

**测试完成后**，统计 ❌ 项数量，每个失败项截图说明操作步骤和实际结果，汇总发给我。
